# HyprVoice V2 UI

This directory will contain the GTK4 overlay and right-side chat panel implementations for HyprVoice V2.

**Note:** No UI code is implemented yet. The UI logic is intentionally separated and decoupled from the backend logic found in `hyprvoice/core/`.
